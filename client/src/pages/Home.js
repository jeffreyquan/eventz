import React from 'react';
import { Grid, Menu } from "semantic-ui-react";

const Home = () => {
  return (
    <Grid container centered style={{ height: "100vh" }}>
      <Grid.Row>
        <Grid.Column width={8} verticalAlign="middle">
          Home Page coming soon
        </Grid.Column>
      </Grid.Row>
    </Grid>
  );
}

export default Home;